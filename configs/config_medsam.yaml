model:
  name: "MedSam"
  in_channels: 3
  out_channels: 1
  model : "../saved_models/medsam/medsam_vit_b.pth"

data:
  train_img_path: "../data/train/original/"
  train_label_path: "../data/train/ground_truth/"
  test_img_path: "../data/test/original/"
  test_label_path: "../data/test/ground_truth/"
  image_size: 1024 #min(image_size) doit être égal à 1024 pour MedSAM
  train_split: 0.8

paths:
  save_dir: "../saved_models/medsam"